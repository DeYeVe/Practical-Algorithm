// https://www.acmicpc.net/problem/5427

#include <bits/stdc++.h>
using namespace std;

#define X first;
#define Y second;

int main(void)
{
	ios::sync_with_stdio(0);
	cin.tie(0);

	int dx[4] = { -1, 0, 1, 0 };
	int dy[4] = { 0, -1, 0, 1 };

	int n; 
	cin >> n;

	while (n--) 
	{
		int vis[1001][1001]; // # = -2, * = -1, . = 0, @ = 0,1,2... 
		int w, h;
		cin >> w >> h;

	}
}